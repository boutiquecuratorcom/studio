rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and should not be used in production.
    // They are for initial setup and testing only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
