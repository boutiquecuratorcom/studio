rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper function to check ownership
    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    // Deny all access by default
    match /{document=**} {
      allow read, write: if false;
    }

    // Allow users to read and write their own user-specific documents
    match /users/{userId}/{document=**} {
      allow read, write: if isOwner(userId);
    }
    
    // Rules for the root 'inventory' collection
    match /inventory/{itemId} {
      // Allow users to read their own items (for both lists and single documents)
      allow read: if isOwner(resource.data.ownerId);
      
      // Allow users to create, update, and delete their own items
      allow write: if isOwner(request.resource.data.ownerId) || isOwner(resource.data.ownerId);
    }
  }
}
