rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all reads and writes.
    // Add specific rules for your collections as you build your app.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
